/**
 * Shared motion constants. Orchestrated sequences use framer-motion;
 * simple hovers stay on CSS (`hover-lift`, duration-hover).
 */
export const hoverDurationMs = 180;

export const reducedMotionTransition = { duration: 0 } as const;
